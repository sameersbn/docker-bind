machine:
  services:
    - docker

dependencies:
  override:
    - docker info

test:
  override:
    - docker build -t sameersbn/bind .
    - docker run -d -p 172.17.0.1:53:53/udp sameersbn/bind; sleep 10
    - host www.google.com 172.17.0.1
